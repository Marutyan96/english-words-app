{
  "compilerOptions": {
    "baseUrl": "./",  // Базовый путь для разрешения модулей
    "paths": {
      "@/*": ["./src/*"]  // Алиас @ для пути к папке src
    }
  },
  "exclude": [
    "node_modules",  // Исключаем папку node_modules
    "dist"          // Исключаем папку сборки dist
  ]
}